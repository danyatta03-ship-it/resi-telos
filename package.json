{
  "name": "resi-telos",
  "version": "1.0.0",
  "description": "Reparto Resi Telos SPA - App statica + api/gemini proxy",
  "private": true,
  "scripts": {
    "dev": "node dev-server.js",
    "check": "node scripts/check-html.js",
    "test": "node --test tests/",
    "bump-cache": "node scripts/bump-cache.js",
    "precommit": "npm run check && npm test"
  },
  "devDependencies": {
    "express": "^4.18.2",
    "ws": "^8.14.2"
  }
}
